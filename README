Приложение получает на вход в виде аргумента путь к текстовому файлу input.txt в формате:
n(3) - количество строк в первом массиве, например 3
Предложение один
Предложение два
Предлоржение три
n(2) - количество строк во втором массиве, например 2
Предложение один
Предложение два
Далее, приложение преобразует исходные строки в строки, состоящие из лемм,
и поочередно сравнивает строки из первого массива со строками из второго
массива. При этом две строки из первого массива не могут ссылаться на одинаковую
строку из второго массива и наоборот. Если остаются строки, у которых нет ни одной
похожей леммы - пара им находится случайно. Также, остаются строки, у которых нет
пары совсем. Например, если один массив больше другого на 3 строки, то у 3 строк
не будет пары. Таким строкам присваиваются восклицательные знаки. Результат
выполнения записываются в ту же папку, где лежит input.txt с названием output.txt.
Проект собирается в jar-архив командой mvn install.
